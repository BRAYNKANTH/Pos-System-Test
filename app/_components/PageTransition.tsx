import type { ReactNode } from "react";
/** Route loading boundaries provide feedback without delaying navigation. */
export function PageTransition({ children }: { children: ReactNode }) { return <>{children}</>; }
