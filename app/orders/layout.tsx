import InventoryLayout from "../inventory/_components/InventoryLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <InventoryLayout>{children}</InventoryLayout>;
}
