# Owner: Shared — flag before editing

RBAC middleware used by every module's protected routes (`checkPermission`).
Owned in practice by Person 5 (Admin Settings), but any module may need to
read from it. Flag changes in PR review before editing.
