# Owner: Shared — flag before editing

Shared UI (approval-queue pattern, modals, badges) — used first by Modules 2
and 3, but available to everyone. Flag changes in PR review before editing
existing components; adding a new component in `components/ui/` is fine.
